<div class = "progress_update">
  <% total = @progress.size %>
  <% @progress.each_with_index do |progress_update, i| %>
    <div class="progress-update">
      <% if (total - i) > @weeks %>
        <header><strong> Extra <%= (total - @weeks - i)%> </strong></header>
      <% else %>
       <header><strong> Update <%= (total - i)%> </strong></header>
      <% end %>

      <p><strong>Date:</strong> <%= progress_update.date %></p>
      <p><strong>Rating:</strong> <%= progress_update.rating %></p>
      <p><strong>Feedback:</strong> <%= progress_update.feedback %></p>
      <p><strong>Created At:</strong> <%= progress_update.created_at.strftime("%d-%m-%Y") %></p>


      <% if @current_instance.supervisor == current_user %>
        <%= link_to "← Edit Progress Update", edit_course_project_progress_update_path(@course, @project, progress_update) %>
        <br>
        <br>
        <%= button_to "Delete",
        course_project_progress_update_path(@course, @project, progress_update),
        method: :delete,
        class: "delete-btn" %>
      <% end %>
      <br>
    </div>
  <% end %>
</div>
